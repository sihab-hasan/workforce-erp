map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      '';
}

# Rate limiting zones
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=1r/s;

# Upstreams
upstream workforce_web { server web:80; keepalive 16; }
upstream workforce_erp { server erp:80; keepalive 16; }
upstream workforce_admin { server admin:80; keepalive 16; }
upstream workforce_api { server api-nginx:80; keepalive 16; }

# Gzip compression
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_comp_level 6;
gzip_types text/plain text/css text/xml application/json application/javascript application/xml application/rss+xml image/svg+xml;

server {
    listen 80 default_server;
    server_name _;
    server_tokens off;

    location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
    }

    location ^~ /.well-known/acme-challenge/ {
        include /etc/nginx/snippets/acme.conf;
    }

    location / { return 444; }
}

server {
    listen 80;
    server_name ${WEB_HOST};
    server_tokens off;
    client_max_body_size ${NGINX_CLIENT_MAX_BODY_SIZE};

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location ^~ /.well-known/acme-challenge/ { include /etc/nginx/snippets/acme.conf; }
    location ^~ /api/v1/auth/ {
        limit_req zone=auth_limit burst=5 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location ^~ /api/ {
        limit_req zone=api_limit burst=20 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location ^~ /sanctum/ {
        limit_req zone=api_limit burst=20 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location / { include /etc/nginx/snippets/proxy-common.conf; proxy_pass http://workforce_web; }
}

server {
    listen 80;
    server_name ${ERP_HOST};
    server_tokens off;
    client_max_body_size ${NGINX_CLIENT_MAX_BODY_SIZE};

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location ^~ /.well-known/acme-challenge/ { include /etc/nginx/snippets/acme.conf; }
    location ^~ /api/v1/auth/ {
        limit_req zone=auth_limit burst=5 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location ^~ /api/ {
        limit_req zone=api_limit burst=20 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location ^~ /sanctum/ {
        limit_req zone=api_limit burst=20 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location / { include /etc/nginx/snippets/proxy-common.conf; proxy_pass http://workforce_erp; }
}

server {
    listen 80;
    server_name ${ADMIN_HOST};
    server_tokens off;
    client_max_body_size ${NGINX_CLIENT_MAX_BODY_SIZE};

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location ^~ /.well-known/acme-challenge/ { include /etc/nginx/snippets/acme.conf; }
    location ^~ /api/v1/auth/ {
        limit_req zone=auth_limit burst=5 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location ^~ /api/ {
        limit_req zone=api_limit burst=20 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location ^~ /sanctum/ {
        limit_req zone=api_limit burst=20 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location / { include /etc/nginx/snippets/proxy-common.conf; proxy_pass http://workforce_admin; }
}

server {
    listen 80;
    server_name ${API_HOST};
    server_tokens off;
    client_max_body_size ${NGINX_CLIENT_MAX_BODY_SIZE};

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location ^~ /.well-known/acme-challenge/ { include /etc/nginx/snippets/acme.conf; }
    location ^~ /api/v1/auth/ {
        limit_req zone=auth_limit burst=5 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
    location / {
        limit_req zone=api_limit burst=20 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://workforce_api;
    }
}
