# @workforce-erp/contracts

Stable cross-boundary TypeScript contracts for API, identity, tenancy, authorization and events.

This package exposes only its public API through `src/index.ts`. Do not deep-import internal files from applications.
