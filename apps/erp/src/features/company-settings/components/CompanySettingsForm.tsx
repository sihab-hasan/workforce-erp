export interface CompanySettingsFormProps {
  className?: string;
}

export function CompanySettingsForm({ className }: CompanySettingsFormProps) {
  return <section className={className} data-component="CompanySettingsForm" />;
}
