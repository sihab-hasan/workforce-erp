export interface SolutionWorkflowProps {
  className?: string
}

export function SolutionWorkflow({ className }: SolutionWorkflowProps) {
  return <section className={className} data-component="SolutionWorkflow" />
}
