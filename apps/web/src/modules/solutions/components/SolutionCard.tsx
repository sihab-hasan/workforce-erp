export interface SolutionCardProps {
  className?: string
}

export function SolutionCard({ className }: SolutionCardProps) {
  return <section className={className} data-component="SolutionCard" />
}
