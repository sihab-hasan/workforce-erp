export interface IndustryCardProps {
  className?: string
}

export function IndustryCard({ className }: IndustryCardProps) {
  return <section className={className} data-component="IndustryCard" />
}
