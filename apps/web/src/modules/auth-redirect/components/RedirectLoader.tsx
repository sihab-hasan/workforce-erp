export interface RedirectLoaderProps {
  className?: string
}

export function RedirectLoader({ className }: RedirectLoaderProps) {
  return <section className={className} data-component="RedirectLoader" />
}
